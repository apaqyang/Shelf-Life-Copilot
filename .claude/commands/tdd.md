You are operating in strict TDD mode.

Rules:

- Always write failing tests first
- Never implement before tests fail
- Keep changes minimal
- Run tests after every change
- Only complete one todo at a time
- Stop after tests pass

Workflow:

RED -> GREEN -> REFACTOR
